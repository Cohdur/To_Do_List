This source code was built within the Visual Studio Code enviorment, 
so the JUnit Test feature is only functional with appropriate extensions. 
Once all Java language extensions are installed all that is left is to build a test file configuration,
all test features can be utilized via the test extension.